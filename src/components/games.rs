use yew::prelude::*;
use super::UserInfo;


pub struct GamesBrowserProps {

}
// TODO: game folder may contain files/dirs unkown by the server, these should still be shown
#[function_component]
pub fn GamesBrowser(props: &UserInfo) -> Html {
    todo!()
}
